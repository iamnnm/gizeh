<% if flash.any? %>
  <% flash.each do |type, message| %>
    <%= content_tag :div, message, class: "alert #{bootstrap_class_for(type)} sticky fixed-top text-center alert-success py-1" %>
  <% end %>
<% end %>
